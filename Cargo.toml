[package]
name = "ship-test-rust"
version = "0.1.6"
edition = "2021"
description = "ship throwaway: a Rust command that exists to be released"
license = "MIT"
repository = "https://github.com/excelano/ship-test-rust"
publish = false

[dependencies]

[package.metadata.deb]
maintainer = "David M. Anderson <david.anderson@excelano.com>"
section = "utils"

[package.metadata.dist]
dist = true

[profile.dist]
inherits = "release"
lto = "thin"
